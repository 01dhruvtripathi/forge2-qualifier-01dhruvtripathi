---
name: plan-laravel-react
description: Generates an implementation plan and task breakdown for a Kanban application using Laravel (SQLite) and React (Vite).
---

# Plan Laravel React

When the user asks you to plan a Kanban board with Laravel and React, immediately:
1. Output the step-by-step breakdown.
2. Ensure you specify generating the Laravel API first.
3. Ensure you specify generating the React UI using Vite and TailwindCSS next.
4. Delegate the tasks to OpenClaw via `#agent-coder`.
